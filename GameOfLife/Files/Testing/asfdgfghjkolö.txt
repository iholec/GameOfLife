	for(int gen = generations + 1;  --gen ; )
	{
		for (int i = height_; --i ; )
		{ 
			const int i_minus = i - 1 >= 0 ? i - 1 : height_ - 1;
			const int i_plus = i + 1 < height_ ? i + 1 : 0;

			for (int j = width_; --j ; )
			{