			if (field_minus_one_[(i - 1 + len_) % len_] == 'x') // todo modulo not modulo cause modulo shit height_) % height_
			{
				++neighbors;
			}
			if (field_minus_one_[(i + 1 + len_) % len_] == 'x')
			{
				++neighbors;
			}
			if (field_minus_one_[(i - width_ + len_) % len_] == 'x') //todo how here modulo not modulo
			{
				++neighbors;
			}
			if (field_minus_one_[(i + width_ + len_) % len_] == 'x')
			{
				++neighbors;
			}
			if (field_minus_one_[(i - width_ - 1 + len_) % len_] == 'x')
			{
				++neighbors;
			}
			if (field_minus_one_[(i + width_ + 1 + len_) % len_] == 'x')
			{
				++neighbors;
			}
			if (field_minus_one_[(i - width_ + 1 + len_) % len_] == 'x')
			{
				++neighbors;
			}
			if (field_minus_one_[(i + width_ - 1 + len_) % len_] == 'x')
			{
				++neighbors;
			}