				std::getline(gol_file_in, width_string, ',');

				for (char c : width_string)
				{
					if (c >= '0' && c <= '9') {
						width = width * 10 + (c - '0');
					}
					else {
						std::cout << "Bad Input";
						return 1;
					}
				}
				//std::cout << width << std::endl;

				std::getline(gol_file_in, height_string);

				for (char c : height_string)
				{
					if (c >= '0' && c <= '9') {
						height = height * 10 + (c - '0');
					}
					else {
						std::cout << "Bad Input";
						return 1;
					}
				}

				//std::cout << height << std::endl;